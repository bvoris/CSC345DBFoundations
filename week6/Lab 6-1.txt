Other Helpful Queries:
DROP TABLE NONCAT;


select *
FROM NONCAT;

Task 1:
CREATE TABLE NONCAT (
ITEM_ID char(4) NOT NULL PRIMARY KEY,
DESCRIPTION char(30),
ON_HAND int,
CATEGORY CHAR(3),
PRICE DECIMAL(5,2)
);

Task 2:
INSERT INTO NONCAT (ITEM_ID, DESCRIPTION, ON_HAND, CATEGORY, PRICE)
SELECT ITEM_ID, DESCRIPTION, ON_HAND, CATEGORY, PRICE
FROM ITEM
WHERE (CATEGORY!='CAT');

Task 3.
UPDATE NONCAT SET DESCRIPTION = 'Dog Toy Gift Bonanza' 
WHERE ITEM_ID = 'DT12' ;

Task 4.
UPDATE NONCAT SET PRICE = PRICE * 1.05 WHERE CATEGORY = 'BRD' ;

Task 5.
INSERT INTO NONCAT (ITEM_ID, DESCRIPTION, ON_HAND, CATEGORY, PRICE ) 
VALUES('FF17', 'Premium Fish Food', 10, 'FSH', 11.95);

Task 6.
DELETE FROM NONCAT WHERE CATEGORY = 'HOR' ; 

Task 7.
UPDATE NONCAT SET CATEGORY = NULL WHERE ITEM_ID = 'UF39'

Task 8.
Task IN ORACLE

ALTER TABLE NONCAT 
ADD ON_HAND_VALUE NUMBER(7,2);

Task IN MYSQL

ALTER TABLE NONCAT 
ADD COLUMN  ON_HAND_VALUE DECIMAL(7,2) ;
UPDATE NONCAT SET  ON_HAND_VALUE = ON_HAND * PRICE ;

Task 9.
Task IN ORALCE

ALTER TABLE NONCAT 
MODIFY DESCRIPTION VARCHAR(40); 

MySQL
ALTER TABLE NONCAT 
MODIFY DESCRIPTION CHAR(40); 


Task 10.
DROP TABLE NONCAT ;